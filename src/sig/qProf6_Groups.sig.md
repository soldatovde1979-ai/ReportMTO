# qProf6_Groups

Статус: CLEAN
Обновлено: 16.09.2026

## Назначение
Диагностический профиль конвейера `Query-ImportJSON` (шаг `WithGroups`): число строк после `fnComputeGroupMetrics` (добавлен `deltaHours`). В сборку не входит, в `tbDATA` не грузится.

## Процедуры / Функции

### qProf6_Groups (запрос)
- Назначение: профилирование шага групповых метрик.
- Вход: `prmSourcePath` — файл или папка.
- Выход: таблица `#table({"Rows"}, ...)` с числом строк `WithGroups`.
- Побочные эффекты: чтение JSON-файлов; записей нет.
