# MTO Smart Analytics — план действий (ревизия от 24.08.2026, вечер)

> Составлено по итогам сверки `HANDOFF.md` с фактическим содержимым проекта ReportMTO.
> Обновляется по мере закрытия пунктов — не архивный документ.

## -2. Реорганизация структуры v2 выполнена (05.09.2026)

- [x] Документы `docs/` переименованы в kebab-case (`system-spec.md`, `data-contract.md`,
      `architecture.md`, `content-spec.md`, `manifest.md`), инструкции установки собраны в
      `docs/install/`, историческое — в `docs/archive/`.
- [x] `ReportMTO.xlsm`/`ReportMTO_starter.xlsx` вынесены в `build/` (вне git, `.gitignore`
      дополнен `build/`, `data/*`, `*.xlsm`, `*.xlsx`).
- [x] `Data/` -> `data/`; мусорные файлы (`_analysis.txt` и т.п.) убраны; `placeholder.json`
      перенесён в `tests/fixtures/`.
- [x] `tools/` пересобран с нуля: старые разошедшиеся с `src/` копии удалены, оставлены
      только `build-report-mto.ps1` и `sim-pipeline.py`. В `build-report-mto.ps1` путь к
      заготовке точечно исправлен на `build\starter\ReportMTO_starter.xlsx` (хеш в
      `docs/manifest.md` пересчитан).
- [x] `design/` расформирована (референс - в `examples/`, дубль отчёта убран); битые по
      именам файлы `tests/` и пустой worktree `.claude/worktrees/work-plan-ae2c56` (указывал
      на путь до переноса диска, коммитов сверх `main` не имел) - туда же.
- [ ] **Требует ручного удаления.** Подключённая папка не даёт `rm`/`rmdir` в этой сессии -
      всё перечисленное выше как «удалено» физически перемещено с сохранением
      относительного пути в `_to_delete/` (в `.gitignore`/`.kilocodeignore`, в коммит не
      попадёт). Сверить с `plans/reorganize-project.md` и удалить `_to_delete/` вручную.
- [x] `README.md`, `docs/manifest.md` и перекрёстные ссылки на переименованные документы -
      обновлены точечно.

## -1. Аудит проекта 05.09.2026 — файловая гигиена (НОВОЕ)

Сверка того, что реально лежит на диске, с тем, что описывают документы. Раздел «Документация —
приведена в порядок» ниже (п.1) утверждал, что плоские дубликаты убраны — на 05.09.2026 это уже
не так, дубликаты вернулись в других местах. Из того, что можно было исправить точечной правкой
содержимого файлов (без удаления/переименования — см. ограничение ниже), сделано:

- [x] `README.md` был испорчен двойной перекодировкой (UTF-8 → ошибочно как CP1251 → снова UTF-8),
      читался как «РЎС‚СЂСѓРєС‚СѓСЂР°» вместо «Структура»; заодно у части кодовых путей в тексте
      backtick-разметка была потеряна (`\tmp_index.html\` вместо `` `tmp_index.html` ``, местами
      с буквально подставленными управляющими символами `\t`/`\r`). Пересобран заново.
- [x] `src\tmp_index.html` оказался НЕ рабочим шаблоном, а копией `design\reference_example.html`
      (визуальный референс без `<!DOCTYPE html>`/`<head>`/`<meta charset>`) под именем реального
      шаблона. Риск: README шаг 5 предписывает скопировать «`tmp_index.html`» рядом с книгой, и
      можно было по ошибке взять версию из `src\` вместо корневой. Содержимое `src\tmp_index.html`
      заменено на полноценный документ (синхронизировано с корневым `tmp_index.html`, который
      совпадает по хешу с MANIFEST v6/v6.1).
- [x] `docs\system-spec.md` и `docs\data-contract.md` дополнены разделами о том, что реально изменилось в коде
      после этой ревизии (v5 02.09.2026, v6, v6.1) — `prmSourcePath` с meta-записью (M-1),
      переформулированный текст ошибки (M-2), переписанный `modPQSync.RefreshImportQuery`
      (S-1/S-2), новые запросы `qExistingData.pq`/`qDiagImport.pq`. Разделы §1–6 самих документов
      пока рассказывают только про ревизию 24.08.2026 — переписывать их целиком под v6.1 отдельной
      точечной правкой не стал, чтобы не внести residual ошибки без возможности проверить в Excel.
- [x] `modContentMTO_v6.1.bas` (в `src\vba\`) оказался побайтово (без учёта переводов строк)
      идентичен `modContentMTO.bas` — лишний файл с версией в имени, что противоречит
      собственному правилу проекта («версия модуля — в комментарии заголовка, не в имени файла»)
      и опасно тем, что при одновременном импорте обоих файлов у VBA-проекта Excel неизбежно
      переименует один модуль (`Attribute VB_Name = "modContentMTO"` совпадает), и контракт
      `modMain → modContentMTO.BuildPivots()` может тихо вызвать не тот модуль. Файл превращён
      в явную пометку-заглушку с указанием, что он устарел и подлежит удалению.
- [ ] **Требует ручного удаления — не выполнено этой правкой.** Сессия работала с проектом через
      подключённую папку Google Диска, и в этой сессии не оказалось доступа к удалению/
      переименованию файлов на диске (мог только читать и перезаписывать содержимое). Удалить
      вручную:
      - `src\vba\modContentMTO_v6.1.bas` — дубликат, содержимое заменено на пометку «УСТАРЕЛО»,
        сам файл нужно стереть после удаления не потеряв ничего (полный текст модуля цел
        в `modContentMTO.bas`).
      - Папка `tools\` целиком, ЕСЛИ она не нужна как отдельный вывод чужого скрипта — на
        05.09.2026 внутри неё лежат старые копии `modMain.bas` (без правок M-1/M-2),
        `modContentMTO.bas` (837 строк вместо 901 в `src\vba\`), `BuildReportMTO.ps1` (290 строк
        против 389 в актуальном `tools_Build-ReportMTO.ps1` в корне — 144 строки различий),
        а также устаревшие собранные `ReportMTO.xlsm`/`ReportMTO_starter.xlsx`. Текстовые файлы
        внутри `tools\`, которые удалось безопасно синхронизировать содержимым без переименования
        (`modMain.bas`, `modContentMTO.bas`, `modAggregate.bas`, `Query-ImportJSON.pq`,
        `fnComputeKey.pq`, `BuildReportMTO.ps1`), — обновлены до актуального содержимого этой
        правкой; бинарные `.xlsm`/`.xlsx` внутри `tools\` — нет (риск повредить книгу, содержимое
        не текстовое).
      - `tests\╨Ю╨Ц╨Ш╨Ф╨Р╨Х╨Ь╨Ю╨Х_v1.md` и `tests\естовый набор v1.md` — файлы с повреждёнными/
        усечёнными именами (похоже на «Тестовый набор v1.md» с потерянной первой буквой);
        сессия не смогла их прочитать/переименовать через доступные инструменты.
      - `Data\_analysis.txt`, `Data\_analyze.py`, `Data\_analyze2.py`, `Data\_check.txt`,
        `Data\placeholder — копия.json` — рабочий мусор в папке, предназначенной только под
        выгрузки 1С (по README, не версионируется).

## 0. Что уже проверено в этой сессии

При первом чтении обнаружилось расхождение: `HANDOFF.md` утверждал, что 5 пробелов
(Блок 2/3/6/7-8) закрыты через новый generic-модуль `modAggregate.bas`, но в проекте
на тот момент modAggregate.bas отсутствовал как файл, а сохранённый `modContentMTO.bas`
был ещё дозакрытой версией (`BuildBlock3KPI`/`Block6RankedTable` — заглушки-TODO).
Также в проекте существовало по два документа `content-spec.md` и
`architecture.md` под одним путём с разным содержимым.

Пользователь обновил файлы проекта. Повторная проверка подтвердила:

- [x] `modAggregate.bas` появился в проекте, реализует `GroupCount`, `GroupCountDistinct`,
      `GroupAverage`, `SortDictionaryKeysByValue` — как и описано в Core.
- [x] `modContentMTO.bas` переписан: `BuildBlock3KPI`, `BuildBlock2Table`,
      `Block6RankedTable`, `BuildSyncPairs`/`Block7ToCompactText`/`Block8ToCompactText`,
      `PivotToHTMLTableWithPercent` — реализованы не как заглушки, реально вызывают
      `modAggregate`.
- [x] Дубликаты `content-spec.md` / `architecture.md` устранены —
      в проекте по одному документу на путь.
- [x] `architecture.md` (§4, §9, §10) описывает `modAggregate.bas` как
      существующий Core-модуль — теперь это соответствует реальному коду.

Файлы также разложены в структуру `src/vba/` + `src/powerquery/` (плоские дубликаты
в корне проекта удалены) — соответствует файловой карте HANDOFF §3 и README, который
генерирует `setup_project_v2.ps1`.

Блокирующее рассогласование снято, можно двигаться дальше.

## 1. Документация — приведена в порядок (ЗАКРЫТО)

- [x] `content-spec.md` → v3.0.1: убран противоречивый абзац конца §5 (утверждавший
      «Блок 3 — заглушка» и т.п. при закрытом §11.1), добавлена пометка про Блок 6
      (окно/история — см. п.3), исправлено ложное «сериализация Блока 8 не реализована»
      в §7 (реализована через `Block8ToCompactText`), добавлена заметка в §10 про
      `prmSourcePath`.
- [x] `system-spec.md` — пересобран: таблица стека (добавлены modPivotBuilder/modAggregate/
      modColor, исправлено имя modHTMLEngine), дерево файлов приведено к фактическому
      (src/vba 9 файлов, src/powerquery 5 файлов, добавлены tools/ и стартовая книга),
      §4.1 переписан под исправленный механизм `prmSourcePath`, добавлен новый §4.6
      «Сборка книги из исходников» (ручная и автоматическая через `tools/Build-ReportMTO.ps1`).
- [x] `data-contract.md` — добавлен §1.2 «Параметр prmSourcePath» с полным контрактом чтения/записи
      и историей бага, обновлена таблица блоков §2.4 (колонка «Реализация»: PivotTable vs
      modAggregate), §3.3 отмечена как сверенная 1:1 с `tmp_index.html`.

## 2. Приоритет 1 — первый реальный прогон в Excel/Power Query (В ПРОЦЕССЕ)

Единственный пункт из открытых вопросов HANDOFF, который **не имеет отношения к
рассинхрону файлов** — сборка и первый прогон в настоящем Excel. Пользователь начал
сборку, прогон уже нашёл первую реальную ошибку (см. ниже) — это подтверждает, что
шаг был не лишним.

- [x] Собрать заготовку книги (`ReportMTO_starter.xlsx` → Save As `.xlsm`): листы
      `Main`/`Variable`/`Logs`/`tbDATA`, таблицы `tblVariable`/`tbLogs`, значения
      `Variable` предзаполнены.
- [x] Импортировать `.bas` из `src/vba/` через редактор VBA.
- [x] Создать 4 функции Power Query (`fnNormalizeFields`, `fnComputeKey`,
      `fnComputeGroupMetrics`, `fnUpsert`) как Blank Query.
- [x] **Найден и исправлен реальный баг**: `prmSourcePath` — это обычная Power Query
      query (коллекция `ThisWorkbook.Queries`), а не именованный диапазон Excel.
      Исходный `modMain.LoadSourceFile` менял `ThisWorkbook.Names("prmSourcePath").RefersTo`
      — это не влияло на то, что реально читает `Query-ImportJSON` через
      `File.Contents(prmSourcePath)`. Исправлено на новую приватную процедуру
      `SetSourcePathParameter`, которая делает
      `ThisWorkbook.Queries("prmSourcePath").Formula = """<путь>"""` — код в проекте
      и README (`setup_project_v2.ps1`) обновлены.
- [x] Заготовка `ReportMTO_starter.xlsx` пересобрана (v2): убран ошибочный
      `DefinedName` `prmSourcePath` из первой версии, добавлена текстовая пометка на
      листе Main о правильном способе создания параметра. Отправлена пользователю.
- [x] Написан `tools/Build-ReportMTO.ps1` — PowerShell COM-скрипт автосборки книги из
      исходников (см. §4 ниже) и отправлен пользователю.
- [ ] Проверить исправленный `SetSourcePathParameter` в `modMain.bas` на практике:
      выбрать файл через «Загрузить», убедиться, что `Query-ImportJSON` при Refresh
      реально подхватывает новый путь.
- [ ] Первый Close & Load To → Table на лист `tbDATA` для `Query-ImportJSON` —
      проверить, что подключение получило имя ровно «Query - ImportJSON» (с пробелами).
- [ ] Прогнать `fnNormalizeFields` / `fnComputeKey` / `fnComputeGroupMetrics` в реальном
      редакторе M-кода — помечены как "иллюстративные, не проверенные", вероятное место
      следующих синтаксических правок (`Table.Group`, `Table.TransformColumns`).
- [ ] Подготовить тестовый (обезличенный) JSON-набор, покрывающий граничные случаи:
      - `post` в разных регистрах ("СТК"/"стк"/"Стк") — проверка `fnNormalizeFields`;
      - >2 статусных записей на пару `(number, direction)` — проверка допущения
        `min`/`max` в `deltaHours`;
      - повторная загрузка файла с изменёнными значениями по тем же `Key` — проверка
        полного upsert (`fnUpsert.pq`), а не anti-join;
      - `number`, присутствующий только в одной из дирекций — проверка, что
        `BuildSyncPairs` в `modContentMTO.bas` корректно его пропускает, а не падает.
- [ ] Прогнать чек-лист приёмки §11 `content-spec.md` целиком по результатам теста.

## 3. Приоритет 2 — точечная проверка логики Блока 6

`content-spec.md` §5 описывает "% планшет" в Блоке 6 как "текущая неделя/окно",
но `Block6RankedTable` в `modContentMTO.bas` считает `% планшет` по всей истории
(`filters:=Array("in_bounds=True", "arm<>")`, без ограничения по `week_status`/окну).

- [ ] Решить: рейтинг инженеров должен быть за всё время (как сейчас в коде) или за
      `REPORT/WEEKS_WINDOW`/текущую неделю (как в описании)? Если нужно окно — добавить
      фильтр по `week_status` в оба `GroupCount` внутри `Block6RankedTable`, аналогично
      тому, как это уже сделано в `BuildBlock3KPI`.

## 4. Инструменты сборки/разработки (НОВОЕ, добавлено 24.08.2026)

По запросу пользователя проведена ревизия инструментов для разработки в VS Code и
автоматической сборки книги:

- **VS Code ↔ Excel:**
  - `ewc3labs.excel-power-query-editor` — извлекает M-код в `.m`-файлы, режим
    «Intelligent Auto-Watch» пишет изменения прямо в `.xlsm`/`.xlsb` без открытого Excel.
    Рекомендован для `src/powerquery/*.pq`.
  - `markuskiller/vba-edit` (PyPI CLI, xlwings/COM) — режим watch пишет изменения `.bas`
    в ОТКРЫТУЮ книгу Excel по сохранению файла. Рекомендован для `src/vba/*.bas`.
  - `PowerQuery.vscode-powerquery` (официальный MS) — только подсветка/статический
    анализ M, без синхронизации; можно держать вместе с ewc3labs как language server.
  - VBA-подсветки (serkonda7/spences10/skyran) — чисто косметические, не заменяют vba-edit.
- **Автосборка:** `tools/Build-ReportMTO.ps1` — PowerShell + COM-автоматизация Excel:
  1) заготовка → `.xlsm`, 2) импорт всех `.bas` (`VBComponents.Import`, задокументированный
  API), 3) создание 6 Power Query-запросов (`Workbook.Queries.Add`, задокументированный API,
  порядок: `prmSourcePath` → 4 функции → `Query-ImportJSON`), 4) ⚠️ ЭКСПЕРИМЕНТАЛЬНАЯ попытка
  автоматического «Close & Load To → Table» через недокументированную строку подключения
  OLEDB;Provider=Microsoft.Mashup.OleDb.1 — обёрнута в try/catch, при неудаче не портит уже
  сделанные шаги 1–3, шаг делается вручную. Требует включённого «Доверять доступ к объектной
  модели проектов VBA». Не прогонялся на реальном Excel самим автором скрипта — первый прогон
  должен быть на копии проекта.
- [x] Прогнан `tools/Build-ReportMTO.ps1` на реальном Windows+Excel пользователем — найдены и
      исправлены два реальных бага (см. ниже), скрипт обновлён и переотправлен.
      **Шаг 4 (Load To Table) до реального теста ещё не дошли** — падение произошло раньше,
      на шаге 2 (импорт VBA-модулей); этот пункт остаётся открытым до следующего прогона.
- [x] **Найден и исправлен баг №1**: `$wb.Workbooks.Open($starterXlsx)` падал с
      `COMException` («Невозможно получить свойство Open класса Workbooks»). Причина не
      установлена окончательно (COM-ошибка без диагностического текста), но воспроизводимо
      снималась стандартным чек-листом Excel-automation (PowerShell не «от имени
      администратора», отсутствие зависшего фонового `EXCEL.EXE`, файл не заблокирован
      Windows как «скачанный из интернета» — снять блокировку через Свойства файла).
- [x] **Найден и исправлен баг №2**: импорт VBA-модулей падал на первом же файле
      (`modAggregate.bas`) с `Невозможно вызвать метод для выражения со значением NULL`
      внутри `$wb.VBProject.VBComponents.Import(...)`, при этом более ранняя проверка
      `$wb.VBProject.VBComponents.Count` проходила успешно — т.е. чтение доступно, а запись
      в VBA-проект через тот же объект — нет. Скрипт исправлен: `VBProject` теперь кэшируется
      в переменную один раз сразу после `SaveAs` (плюс `Start-Sleep -Milliseconds 500` перед
      первым обращением к нему), каждый `Import` обёрнут в свой `try/catch` с понятным
      сообщением. Если ошибка повторится и после этого — вероятная причина зафиксирована в
      сообщении скрипта: групповая политика организации может принудительно сбрасывать
      реестровый ключ `AccessVBOM`, даже когда галочка «Доверять доступ к объектной модели
      проектов VBA» в интерфейсе выглядит включённой — это уже вопрос к ИТ-администратору,
      не к скрипту.
- [ ] Дождаться следующего прогона исправленной версии скрипта — подтвердить, что импорт
      всех 9 модулей и создание всех 6 Power Query-запросов проходят целиком, и наконец
      дойти до шага 4 (Load To Table), который пока не проверялся ни разу.

## 5. Приоритет 3 — вопросы, требующие решения заказчика (не technical debt)

Список не изменился со времени HANDOFF, но теперь это единственное, что реально
блокирует финализацию (после того как п.2 будет пройден):

- [ ] Формально согласовать замену ИИ-провайдера Gemini → DeepSeek.
- [ ] Подтвердить, действительно ли в реальных данных 1С встречаются >2 статусных
      записи на пару `(number, direction)` — от этого зависит, нужна ли доработка
      допущения `min`/`max` в `deltaHours`.
- [ ] Решить по `Variable/REPORT/SYNC_THRESHOLD_MIN`: оставить как есть (среднее время
      расхождения, без порога) или добавить долю/флаг "не синхронизировано" в
      `Block7ToCompactText`/`Block8ToCompactText` (в коде это явно оставлено как
      сознательный выбор, но не подтверждено заказчиком).
- [ ] Подтвердить трактовку "текущая неделя" в Блоке 3 как максимального `week_status`,
      реально присутствующего в данных (а не календарной недели).
- [ ] Сверить точные имена HTML-плейсхолдеров `{{BLOCK_N_...}}` с ожиданиями заказчика.
- [ ] Обосновать/подтвердить `TOPN = 10` как окончательное значение (сейчас — из
      формулировки ТЗ "топ-10", не переподтверждено отдельно).

## 6. Бэклог (вне пилота, без срочности)

Из `system-spec.md` §6 — переносится сюда без изменений, чтобы не потерялось:

- [ ] Ротация файлов в `result\` (хранить последние N отчётов).
- [ ] Формализовать состав KPI Блока 3 с заказчиком отдельным согласованием.
- [ ] Перенести Core-часть платформы на новое направление отчётности (проверка
      переиспользуемости, см. Core §16).
- [ ] Согласовать с ИБ регламент доступа к `result\` (права NTFS/сетевой папки).
- [ ] Опционально: копия сводных таблиц в `Report_<дата>.xlsx`.

## 7. Справочные материалы (НОВОЕ)

- [x] Опубликована наглядная схема «Архитектура ReportMTO» — бизнес-процесс с
      привязкой модулей к шагам + граф модулей Core/Content Spec с Function Contract.
      Держать в актуальном виде вместе с правками Core/Content Spec.

## 8. Рекомендуемый порядок

1. ~~Поправить §5 `content-spec.md`~~ — сделано (см. §1).
2. Продолжить сборку и прогон кода на тестовых данных (раздел 2) — сейчас в процессе;
   это единственный способ узнать реальный объём доработок перед тем, как
   согласовывать что-либо с заказчиком.
3. Опробовать `tools/Build-ReportMTO.ps1` на копии проекта, чтобы сборка следующих
   направлений (см. Core §16) не требовала повторения всех ручных шагов.
4. По итогам прогона — точечно поправить код (если вскроются синтаксические ошибки
   M-кода или пограничные случаи — как уже произошло с `prmSourcePath`).
5. Собрать пункты раздела 5 в один список вопросов и вынести на согласование с
   заказчиком одним заходом, а не по одному.
6. Бэклог (раздел 6) — после пилота, не блокирует сдачу.
