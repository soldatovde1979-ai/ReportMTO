# Постановка по отчёту — задачи (версия 2)

> Версия 2 от 14.09.2026. Преобразование [`docs/task-rep.md`](task-rep.md): весь текст
> постановки разбит на задачи без потерь (контроль переноса — в конце файла).
> Отметки `[x]`/`[ ]` части 1 — по результатам перепроверки кода
> ([`src/vba/modContentMTO.bas`](../src/vba/modContentMTO.bas),
> [`src/vba/modContentZone.bas`](../src/vba/modContentZone.bas),
> [`tmp_index.html`](../tmp_index.html), `build\tmp_index.html`).
> Часть 2 перепроверена по коду и шаблонам 16.09.2026 — отметки `[x]` подтверждены,
> замечания внесены в текст части 2. Часть 3 дополнена новыми задачами 2–8 от 16.09.2026.

## Часть 1

### Общие доработки (шапка)

- [x] **1. Верх лево** — заменить на «Исполнительская дисциплина инженеров ДЭНТ и ДГМ».
  Реализовано: `REPORT_TITLE` — [`modContentMTO.bas:4095`](../src/vba/modContentMTO.bas:4095).
- [x] **2. Подзаголовок** — «Отчет о состоянии техники за {отчетная неделя} — с {дата начала
  отчетной недели} до {дата конца отчетной недели}», все плейсхолдеры выделить белым жирным.
  Реализовано: [`BuildLede()`](../src/vba/modContentMTO.bas:4150) оборачивает неделю и даты
  в `<span class="hl">`; класс `.hl` — [`tmp_index.html:59`](../tmp_index.html:59).
- [x] **3. Верх право** — вставить надпись «С начала года:».
  Реализовано: `facts-cap` — [`tmp_index.html:212`](../tmp_index.html:212).
- [x] **4.** «СОБЫТИЙ» заменить на «СОБЫТИЙ (ПЛАНШЕТ, ПК)».
  Реализовано: «Событий (планшет, ПК)» — [`modContentMTO.bas:4164`](../src/vba/modContentMTO.bas:4164).
- [x] **5.** «НАРЯДОВ» — не трогать.
  Оставлено: «Нарядов» — [`modContentMTO.bas:4166`](../src/vba/modContentMTO.bas:4166).
- [x] **6.** Сделать третью колонку фактов.
  Реализовано: шесть фактов в сетке 3×2 — `.facts` [`tmp_index.html:54`](../tmp_index.html:54),
  [`BuildFactsRef()`](../src/vba/modContentMTO.bas:4162).
- [x] **7.** «ОТКРЫТО ЗН > мес. {открытых заказ нарядов больше месяца}».
  Реализовано: ярлык «Открыто ЗН > мес.» — [`modContentMTO.bas:4168`](../src/vba/modContentMTO.bas:4168);
  счётчик `OpenOverMonth()` — наряды без `zn_closed` старше 30 суток от конца снимка
  ([`modContentZone.bas:3767`](../src/vba/modContentZone.bas:3767)).
- [x] **8.** «МАШИН В ПАРКЕ» заменить на «ТС по ЗН».
  Реализовано: «ТС по ЗН» — [`modContentMTO.bas:4170`](../src/vba/modContentMTO.bas:4170).
- [x] **9.** «БЕЗ ПОСТА» — оставить.
  Оставлено: «Без поста» — [`modContentMTO.bas:4172`](../src/vba/modContentMTO.bas:4172).
- [x] **10.** Третья колонка — «ПОВТОРНЫЕ {повторных заездов с начала года}».
  Реализовано: «Повторные» — [`modContentMTO.bas:4174`](../src/vba/modContentMTO.bas:4174);
  счётчик `RetCount7()` — возвраты по группе дефекта, окно 7 суток, с начала года
  ([`modContentZone.bas:3780`](../src/vba/modContentZone.bas:3780)).

### Слайд 1 — плитки

- [x] **11.** «НАРЯДОВ ОТКРЫТО» — не трогать. Оставлено: «Нарядов открыто» —
  [`modContentZone.bas:3434`](../src/vba/modContentZone.bas:3434).
- [x] **12.** «ЗАКРЫТО» — не трогать. Оставлено: «Закрыто нарядов» —
  [`modContentZone.bas:3436`](../src/vba/modContentZone.bas:3436).
- [x] **13.** Поставить «ВИСИТ НА КОНЕЦ НЕДЕЛИ».
  Реализовано: «Висит на конец недели» — [`modContentZone.bas:3438`](../src/vba/modContentZone.bas:3438).
- [x] **14.** «Без поста за неделю».
  Реализовано: [`modContentZone.bas:3440`](../src/vba/modContentZone.bas:3440).
- [x] **15.** «ЗАЕЗДОВ ТЕХНИКИ».
  Реализовано: «Заездов техники» — [`modContentZone.bas:3442`](../src/vba/modContentZone.bas:3442).
- [x] **16.** «ТС по ЗН {уникальных машин за неделю}».
  Реализовано: «ТС по ЗН», счётчик уникальных ТС по неделям (`uniqVeh`) —
  [`modContentZone.bas:3444`](../src/vba/modContentZone.bas:3444).
- [x] **17.** «МЕДИАНА *».
  Реализовано: «Медиана в ремзоне *» — [`modContentZone.bas:3446`](../src/vba/modContentZone.bas:3446).
- [x] **18.** «ВОЗРАТОВ (7 дн.) *».
  Реализовано: «Возвратов (7 дн.) *» — [`modContentZone.bas:3448`](../src/vba/modContentZone.bas:3448).

### Материалы недели (легенда)

- [x] **19.** Надпись «Плитка «Готово, но не забрано» из эскиза снята: … Разбор на слайде 7.»
  заменить на:
  `* МЕДИАНА — медиана по времени нахождения в ремзоне (длительность между «Готов к выдаче»
  и «Готов к приемке»)` и `* ВОЗРАТОВ — технику вернули в ремонт с аналогичной по
  классификатору дефектов поломкой в течение 7 дней с момента последнего ремонта`.
  Реализовано: `calc-note` слайда 1 — [`tmp_index.html:221`](../tmp_index.html:221)
  (и `build\tmp_index.html`).

### Блоки слайда 1 (раскладка: Д1 Д2 / Б3 Б2 / Б4)

- [x] **20. Д1** — «Висит на конец недели ЗН по Видам ремонта».
  Реализовано: [`BuildHangByZnType()`](../src/vba/modContentZone.bas:3523), плейсхолдер
  `BLOCK_HANG_ZNTYPE` — [`modContentZone.bas:3070`](../src/vba/modContentZone.bas:3070) и шаблон.
- [x] **21. Д2** — «Висит на конец недели по статусу (текущий статус заказ наряда)».
  Реализовано: [`BuildHangByStatus()`](../src/vba/modContentZone.bas:3563), плейсхолдер
  `BLOCK_HANG_STATUS` — [`modContentZone.bas:3071`](../src/vba/modContentZone.bas:3071) и шаблон.
- [x] **22. Б3** — «Какие дефекты были за неделю» (бывший блок Б3).
  Реализовано: [`BuildFlowDefekt()`](../src/vba/modContentZone.bas:3694) фильтрует по
  `Z_WEEK = ZoneReportWeek()`; NoteBlk описывает выборку «за отчётную неделю», без суждений.
- [x] **23. Б2** — «Вид ремонта».
  Заголовок в шаблоне есть, данные — [`BuildFlowZnType()`](../src/vba/modContentZone.bas:3602).
  Примечание: разрез считается за весь период выгрузки (в постановке период не указан).
- [x] **24. Б4** — «Без поста ремзоны».
  Реализовано: секция с `mock-label` «Без поста ремзоны» и `{{BLOCK_NOPOST_WEEKLY}}`
  добавлена после Б3/Б2 слайда 1 в [`tmp_index.html`](../tmp_index.html:221) и
  `build\tmp_index.html` (файлы идентичны).

**Итог части 1:** выполнено 24 из 24 задач.

## Часть 2

### Общее требование (все таблицы и графики)

- [x] **1.** Убрать из комментария к таблицам суждение и сделать поле полезным для всех
  таблиц и графиков — там должны описываться показываемые поля для пользователя
  (алгоритмы сбора, период).
  Реализовано: NoteBlk пересмотрены в modContentZone.bas (слайды 1, 5–8) и
  modContentDisc.bas (слайды 2–4); суждения убраны, тексты содержат поля/алгоритм/период.
  Точечно изменены: BuildRepeats, BuildTimeHist (modContentZone.bas),
  BuildUnsignedOwner (modContentDisc.bas).
  Перепроверка 16.09.2026: описания полей/алгоритма/периода есть у всех таблиц и
  графиков. Остатки: в [`BuildAbc()`](../src/vba/modContentZone.bas:2852) сохранилось
  суждение «заявка формируется по ним»; устаревший построитель
  [`modContentMTO.bas:3183`](../src/vba/modContentMTO.bas:3183) содержит «стоит проверить
  у заказчика» (в активный отчёт не выводится). KPI-плитки слайдов 1, 4–8 описаний не
  имеют — закрывается задачей 8 части 3.

### Слайд 1

- [x] **2.** `TekStatusPoDoc` — убрать слово (из блока «Текущий статус заказ-наряда · TekStatusPoDoc»).
  Реализовано: заголовок блока Д2 — «Текущий статус заказ-наряда» (оба шаблона).
- [x] **3.** После блоков «Висит на конец недели ЗН по видам ремонта» и «Текущий статус
  заказ-наряда» должны идти блоки «Без ремзоны по видам воздействия» и «Без ремзоны по статусу ЗН».
  Реализовано: [`BuildNoZoneZnType()`](../src/vba/modContentZone.bas:3597) →
  `BLOCK_NOZONE_ZNTYPE`, [`BuildNoZoneStatus()`](../src/vba/modContentZone.bas:3631) →
  `BLOCK_NOZONE_STATUS`; выборка — наряды с пустым `postN`, разрез `zn_type` /
  `TekStatusPoDoc`, единица счёта — заказ-наряд; секции в обоих шаблонах.

### Слайд 2

- [x] **4.** Ответить на вопрос: как считаются возвраты в отчётах?
  **Ответ (R4):** возврат = техника вернулась в ремонт с аналогичной по классификатору
  дефектов поломкой в течение 7 дней от `zn_closed` предыдущего наряда. Система умеет
  считать возвраты в окнах 7 и 30 дней; в отчётах этой версии используется 7 дней.
  Код не менялся.
- [x] **5. Таблица 1** — должна быть только по СТК и ПРК:
  - Столбцы: Дирекция / ARM → Нед. 15, Нед. 17, Нед. 23, … Нед. 30 → Всего.
  - Строки (порядок ДГМ → ДЭНТ; в описании ДЭНТ → ДГМ): для каждой дирекции —
    всего → ПЛАНШЕТ → ПК → % планшет.
  - Фильтрация: только последние 8 недель по `status_date` — дате статуса заказ-наряда
    (**R1:** «дата счёт-фактуры» в исходной постановке — опечатка); «ARM = не подписан» — исключаем.
  - **R2 (решение 09.09.2026):** поле `in_bounds` всегда ЛОЖЬ (мёртвое) — фильтр по
    `in_bounds` снят полностью.
  - Значения: Count строк по соответствию arm/дирекции; % планшет = ПЛАНШЕТ / (ПЛАНШЕТ + ПК) * 100.
  - Оформление: цветовая подсветка строки % планшет — «чем больше число тем зеленее,
    чем меньше краснее, посередине жёлтый» (шкала #ef4444 → #f59e0b → #10b981).
  - Строки «Всего» (сумма по неделям) — есть; отдельной строки «Всего %» в постановке нет.
  - **ДЛЯ ДАННОГО СЛАЙДА ВЗЯТЬ ТОЛЬКО ДЭНТ.**
  - Примечание постановки: ранее реализовано в `modContentMTO.bas` — `BuildBlock1Table`
    (строка «% планшет» по неделям дирекции), позднее `BuildWeeksTable` (строки
    всего → % → ПЛАНШЕТ → ПК, столбцы — недели окна `WEEKS_WINDOW`); ТЗ-документы:
    `tz_Report v1.2.md` и `MTO_Презентация_и_блоки_данных_v2.5.md`.
  Реализовано: [`BuildBlock1()`](../src/vba/modContentDisc.bas:310) — плейсхолдеры
  `BLOCK_TABLE1_DENT` / `BLOCK_TABLE1_DGM`; строки Всего → ПЛАНШЕТ → ПК → % планшет,
  столбцы — последние 8 недель по `status_date` (по убыванию) → Всего; шкала
  #ef4444 → #f59e0b → #10b981 через `modColor.PercentToColor`.
- [x] **6. Таблица 2** — отбор: отчётная неделя.
  Столбцы: РемЗона, нарядов, с планшета, % планшетов. Строки — все ремзоны
  (на текущий момент почему-то не все).
  Реализовано (**R3**): [`BuildPostsTable()`](../src/vba/modContentDisc.bas:456) — разрез
  строк `postN` («Пост не указан» отдельной строкой), итоговая строка «Всего» первой
  строкой (всего нарядов / с планшета / общий %), колонка «Оценка» исключена.
- [x] **7. Таблица 3** — оформить поля ПН в виде:
  `|  |ПН-1|` / `|ПН|ПН-2|` / `|  |ПН-3|`;
  у каждого ПН-Х — стрелочку вверх или вниз по сравнению с предыдущей неделей.
  Реализовано: [`BuildPeople()`](../src/vba/modContentDisc.bas:532) — шапка лесенкой по
  разметке постановки; в ячейках ПН-1/ПН-2/ПН-3 стрелка ↑/↓ к предыдущей неделе
  (без предыдущей недели — без стрелки).
  Перепроверка 16.09.2026: стрелки реализованы ([`PctArrowTd()`](../src/vba/modContentDisc.bas:628));
  геометрия лесенки выполнена диагональю (ПН-1/ПН-2/ПН-3 в разных колонках), тогда как
  дословная разметка постановки — ПН-1/ПН-2/ПН-3 друг под другом справа от ПН.
  Расхождение визуальное, приоритет низкий.
- [x] **8. Таблица 4 и отчёт 4** — добавить к названию «Статистика подписания - ДЭНТ - за какой период».
  Реализовано: заголовки «Статистика подписания - ДЭНТ - за весь период» и
  «Статистика подписания - ДГМ - за весь период» — в обоих шаблонах.
- [x] **9. Таблица 5** — название «НЕ ПОДПИСАН по статусу ЗН ДЭНТ».
  Колонки: Текущий статус заказ наряда, количество, процент; справа график.
  Реализовано: `BLOCK_NOSIGN_STATUS_DENT` — [`BuildNoSignSplit()`](../src/vba/modContentDisc.bas:795),
  разрез `TekStatusPoDoc` (поле добавлено в запись `mOrd`, `E_TEK`).
  Перепроверка 16.09.2026: отбор не зависит от дирекции — у наряда без единой подписи
  нет подписей ни одной дирекции, поэтому таблицы 5/6 на слайдах 2 и 3 показывают
  одинаковые данные (зафиксировано в NoteBlk).
- [x] **10. Таблица 6** — название «НЕ ПОДПИСАН по виду ремонта ЗН ДЭНТ».
  Колонки: Вид ремонта, количество, процент; справа график.
  Реализовано: `BLOCK_NOSIGN_TYPE_DENT` — тот же `BuildNoSignSplit`, разрез `zn_type`.
  Отбор обеих таблиц: наряды без единой подписи (`SignedCount = 0`, как на слайде 4);
  двухколоночная разметка «таблица + график».

### Слайд 3

- [x] **11.** То же самое, что Слайд 2, только по ДГМ (названия, фильтры).
  Реализовано: слайд 3 — `BLOCK_TABLE1_DGM`, `BLOCK_NOSIGN_STATUS_DGM`,
  `BLOCK_NOSIGN_TYPE_DGM`; названия и фильтры по ДГМ; слайд 4 не менялся (R5,
  дублирование таблиц 5/6 со слайдом 4 допустимо).

**Б0 (R3). Единый разрез `postN` во всех блоках.** Проверено по слайдам 1–8 и
приведено к `postN`: слайды 2/3 (`BuildWeeksTable`, `BuildBlock1`, `BuildPostsTable`),
слайд 4 (`BuildUnsignedPost`), слайд 5 (`BuildPostsWeek`), слайд 1 (KPI-плитка «Без
поста за неделю», `BuildNoPostWeekly`, `NoPostPct` шапки). Пустой `postN` — отдельной
строкой «(пост не указан)». Колонки «Пост/Площадка» на слайде 7 и реестр дефектов
слайда 8 остались на поле `post` (диагностика дефекта выгрузки, в решение R3 не входили).

**Итог части 2:** перепроверено по коду и шаблонам 16.09.2026 — задачи 1–11 выполнены,
Б0 выполнен. Замечания зафиксированы у задач 1, 7, 9.

## Часть 3

- [x] **1.** Кто чаще заезжает на повторный и какими поломками (часть 3 целиком).
  Реализовано по [`docs/plans/tz_Remzona_Part3_v1.0.md`](plans/tz_Remzona_Part3_v1.0.md)
  в [`modContentZone.bas`](../src/vba/modContentZone.bas:1):
  - «Топ машин по числу повторных заездов» — [`BuildRepeatTopVeh()`](../src/vba/modContentZone.bas:3146),
    плейсхолдеры `BLOCK_REPEAT_TOP_VEH_YTD` / `BLOCK_REPEAT_TOP_VEH_WK`;
  - «Повторы по группам дефектов» — [`BuildRepeatTopDef()`](../src/vba/modContentZone.bas:3174),
    плейсхолдеры `BLOCK_REPEAT_TOP_DEF_YTD` / `BLOCK_REPEAT_TOP_DEF_WK`;
  - «Сколько машина стояла и сколько трудочасов списано» — [`BuildDownVsHours()`](../src/vba/modContentZone.bas:3196)
    (полные пары «Готов к приемке → Готов к выбытию» по дирекциям, `cost_Trudozatrat`, `hourdlit`),
    плейсхолдеры `BLOCK_DOWN_VS_HOURS_YTD` / `BLOCK_DOWN_VS_HOURS_WK`;
  - «Время от создания до приёмки по видам техники» — [`BuildCreateToAcc()`](../src/vba/modContentZone.bas:3270),
    плейсхолдеры `BLOCK_CREATE_TO_ACC_YTD` / `BLOCK_CREATE_TO_ACC_WK`;
  - NoteBlk фаз [`BuildPhases()`](../src/vba/modContentZone.bas:2455) уточнён: «день ремзоны» —
    фазы «ремзона» и «закрытие», фаза «постановка» (создание → приёмка) в него не входит.
  Решения владельца: **R1** — разрез топ-10 `vehicle_group` + строка «прочие»; **R2** — по два
  экземпляра каждого блока (YTD — весь снимок, WK — отчётная неделя, `Z_WEEK = ZoneReportWeek()`);
  **R3** — размещение на слайде 7; **R4** — блок простоя/трудочасов в виде таблицы
  «Машина / Стояла / Списано / План» + бары простоя (три серии на одном графике не делались).
  Установлено в build- и прод-книги, компиляция и прогон отчёта — 15.09.2026.

### Дополнение Части 3 от 16.09.2026 (задачи 2–8)

- [x] **2. Общий фильтр «с начала года».** Все таблицы и графики, которые НЕ являются
 отчётом за отчётную неделю и НЕ являются таблицей/графиком за недели, должны
 фильтроваться по дате заказ-наряда `date` >= 01.01.2026. Недельные разрезы (отчётная
 неделя, окно 8 недель) и служебные реестры (`BLOCK_QUALITY`, `BLOCK_LIMITS`,
 `BLOCK_REQUEST`) фильтру не подлежат. Поштучный список блоков — в
 [`docs/plans/task-rep-v2-часть3-постановка-разработчику.md`](plans/task-rep-v2-часть3-постановка-разработчику.md).
 Реализовано (16.09.2026): `YtdStart()` (кэш ключа `REPORT/YTD_START`, по умолчанию
 2026-01-01), `InYtd(z)` / `InYtdOrd(e)` в [`modContentZone.bas`](../src/vba/modContentZone.bas)
 — применены по списку В1 (блоки «весь снимок», `EnsureFlow`, `RetPairs`,
 `InPeriod`, `RepeatAggregate`, парковые блоки с локальным пересчётом часов/материалов)
 и в [`modContentDisc.bas`](../src/vba/modContentDisc.bas) (блоки слайдов 2–4); недельные
 блоки, реестры качества и шапка не фильтруются. NoteBlk затронутых блоков —
 «с начала года (01.01.2026)».
- [x] **3. Слайд 1 — описание показателей.** В подвале слайда 1 (`calc-note`) описать,
 как формируется каждый показатель KPI-плиток: «Нарядов открыто», «Закрыто нарядов»,
 «Висит на конец недели», «Без поста за неделю», «Заездов техники», «ТС по ЗН»,
 «Медиана в ремзоне», «Возвратов (7 дн.)». Из описаний должно стать ясно, чем
 отличается «Нарядов открыто» (наряды, созданные на отчётной неделе) от «Висит на
 конец недели» (наряды, созданные до конца отчётной недели и не закрытые на её конец).
 Реализовано (16.09.2026): `calc-note` слайда 1 в обоих шаблонах — перечень из 8 строк,
 каждая плитка своей строкой (по решению владельца); «МЕДИАНА» описана по факту кода
 («создание → закрытие» по нарядам, закрытым на отчётной неделе) — прежняя формулировка
 «между «Готов к выдаче» и «Готов к приемке»» коду не соответствовала; «ВОЗРАТОВ» —
 та же группа дефекта в течение 7 суток от закрытия (или создания) предыдущего наряда.
 Звёздочки `*` у плиток сняты (инициатива И3).
- [x] **4. Слайд 1 — график по статусам внизу слайда.** Отдельный график разбивки по
 статусам на выборке «висит на конец недели» (как у блока Д2); статусы «Отменен,
 требует повторного планирования» и «Ожидание ТМЦ» — отдельным небольшим блоком;
 статус «Закрыт (Омникомм)» считать закрытыми (объединять со «Закрыт»).
 Реализовано (16.09.2026): [`BuildHangStatusChart()`](../src/vba/modContentZone.bas)
 (разрез `TekStatusPoDoc`, «Закрыт» + «Закрыт (Омникомм)» → «Закрыт (вкл. Омникомм)»,
 решение **R6**) и [`BuildHangStatusSpecial()`](../src/vba/modContentZone.bas) (два
 статуса отдельной таблицей, % от выборки); плейсхолдеры `BLOCK_HANG_STATUS_CHART`,
 `BLOCK_HANG_STATUS_SPECIAL`; секция внизу слайда 1 в обоих шаблонах. Объединение
 «Закрыт (Омникомм)» со «Закрыт» применено и к блоку Д2 (инициатива И4).
- [x] **5. Фильтр блоков «без ремзоны».** Счётчик и отчёты «без ремзоны» — плитка
 «Без поста за неделю», блок «Без поста ремзоны», блоки «Без ремзоны по видам
 воздействия» / «Без ремзоны по статусу ЗН», шапочный «Без поста» — выбирать наряды
 с фильтром: `zn_type` NOT IN («Обслуживание при выпуске», «Omnicomm») и
 `TekStatusPoDoc` IN («Готов к приемке», «В ремонте», «Готов к выбытию», «Работы
 выполнены», «Закрыт»); «Закрыт (Омникомм)» приравнивается к «Закрыт» и входит в
 допустимые статусы.
 Реализовано (16.09.2026): [`NoZoneOk()`](../src/vba/modContentZone.bas)
 (Trim + vbTextCompare, точные списки) применён в `BuildNoZoneZnType`,
 `BuildNoZoneStatus`, `BuildNoPostWeekly` (np), `Slide1Series` (noPost), `NoPostPct`;
 формулировка выборки дописана в NoteBlk/figcaption блоков.
- [x] **6. Слайды 2 и 3 — название Таблицы 1.** «Таблица 1 · ПЛАНШЕТ и ПК по неделям ·
 ДЭНТ» заменить на «Таблица 1 · ПЛАНШЕТ и ПК по неделям · СТК и ПРК · ДЭНТ»; на
 слайде 3 — то же с ДГМ (оба шаблона).
 Реализовано (16.09.2026): названия в обоих шаблонах; `BuildBlock1` не менялся.
 Дополнительно (инициативы владельца): заголовки «Статистика подписания … за весь
 период» → «… с начала года» (И2); «Аналитика по постам» → «… · все ремзоны …» (И1).
- [x] **7. Слайды 2 и 3 — таблица по подразделениям.** Внизу слайдов 2 и 3 сделать
 таблицу по образцу Таблицы 3 (сотрудники), но только по подразделению (`emp_dep`),
 без сотрудников.
 Реализовано (16.09.2026): агрегаты `mDTot`/`mDTab`/`mDAcc`/`mDLev`/`mDPairs` в
 [`EnsureDisc()`](../src/vba/modContentDisc.bas) и [`BuildDepts()`](../src/vba/modContentDisc.bas)
 по образцу `BuildPeople` (4 недели % планшета со стрелками, справа объём и время за
 отчётную неделю; ср. время — аналог `AvgSpan` на `mDPairs`); плейсхолдеры
 `BLOCK_DEPTS_DENT` / `BLOCK_DEPTS_DGM`; секции внизу слайдов 2/3 в обоих шаблонах.
 Порог включения — подразделения с хотя бы одним событием за отчётную неделю (**R7**);
 пустое подразделение — «(подразделение не указано)». Ключи агрегатов содержат
 дирекцию (решение владельца **R9**, отклонение от буквы ТЗ: иначе слайды 2 и 3
 показали бы одинаковые списки).
- [x] **8. Описания ко всем таблицам и графикам.** Дополнить описания (поля, алгоритм,
 период) ко всем таблицам и графикам, у которых их нет, включая KPI-плитки слайдов
 1, 4–8; убрать остаточное суждение «заявка формируется по ним» из
 [`BuildAbc()`](../src/vba/modContentZone.bas:2852).
 Реализовано (16.09.2026): NoteBlk после KPI-плиток слайдов 4–8
 (`BuildKpiUnsigned`, `BuildKpiFleet`, `BuildRetKpi`, `BuildReturnKpi`, `BuildKpiParts`);
 слайд 1 — задачей 3; в `BuildAbc` суждение заменено на «группа A - 80 % расхода».

### Решения и инициативы Части 3 (16.09.2026)

- **R6:** «Закрыт (Омникомм)» = «Закрыт»; в новом графике внизу слайда 1 — категория
  «Закрыт (вкл. Омникомм)», в блоке Д2 — объединение со «Закрыт».
- **R7:** порог включения подразделений в `BuildDepts` — хотя бы одно событие за
  отчётную неделю.
- **R8:** шапка (`BuildFactsRef`) YTD-фильтром не ограничивается — она уже ограничена
  снимком с 01.01.2026.
- **R9 (решение владельца от 16.09.2026):** ключи агрегатов подразделений содержат
  дирекцию («дирекция|подразделение|неделя») — слайды 2/3 различаются корректно.
- **Инициативы владельца:** И1 — все ремзоны в `BuildPostsTable` (ключ
  `REPORT/ZONES_ALL`, по умолчанию `SLIDE_ZONES` + все postN снимка; нулевые строки),
  «все ремзоны» в названии; И2 — «за весь период» → «с начала года»; И3 — сняты
  звёздочки у плиток «Медиана в ремзоне»/«Возвратов (7 дн.)»; И4 — объединение
  «Закрыт (Омникомм)» в блоке Д2.
- **Грабля дня:** использование переменной цикла до её `Dim` (VBA поднимает объявление,
  компиляция молчит) — добавлена проверка `DIMAFTERUSE` в
  [`tools/vba_lint_v1.0/vba_lint.py`](../tools/vba_lint_v1.0/vba_lint.py) (v1.2) и
  строка в [`docs/rules.md`](../docs/rules.md).

## Контроль переноса

Сверка с [`docs/task-rep.md`](task-rep.md) (113 строк):

- Часть 1, общие доработки (пп. 1–10 исходника) → задачи 1–10 части 1.
- Часть 1, слайд 1 (пп. 1.1–3, «Как сейчас»/«Как надо») → задачи 11–24 части 1;
  раскладка «Как сейчас» (Б1–Б4) отражена в примечании заголовка раздела блоков.
- Часть 2, общее требование → задача 1 части 2; слайд 1 → задачи 2–3; слайд 2 →
  задачи 4–10; слайд 3 → задача 11.
- Часть 3 → задача 1 части 3.
- Часть 3 дополнена 16.09.2026 задачами 2–8 (новые требования владельца, в исходник
  не входили).
- Все тексты исходника перенесены без сокращений смысла; исходник не изменялся.
